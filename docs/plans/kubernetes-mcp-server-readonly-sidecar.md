# Plan: kubernetes-mcp-server as a Read-Only Secondary Downstream (Interim Step)

**Date:** 2026-07-12
**Goal:** Run the upstream `containers/kubernetes-mcp-server` Go binary alongside the existing `InfraGate.McpServer`, exposed through the Gateway as a small, curated, **read-only-only** set of tools, acquired via `go install` of a pinned official release at build time — with zero changes to the approval/mutation path.

## Context

This is the interim step of a longer-term direction already recorded in the repo: **ADR-0021** states the active roadmap plans to eventually swap `InfraGate.McpServer` for `containers/kubernetes-mcp-server` entirely. That full swap is deliberately **not** what this plan does — it's blocked on an open question (does kubernetes-mcp-server's generic apply support a dry-run mode `KubernetesPlanBuilder` can consume for its pre-approval diff?) that this plan does not attempt to resolve.

Instead, this plan adds kubernetes-mcp-server as a **second, independent downstream MCP process**, read-only only, never routed through `IDomainAdapter` / `KubernetesPlanBuilder` / `InfraGate.Approvals`. It exists purely to give the AI broader inspection tools (arbitrary resource kinds, etc.) beyond InfraGate.McpServer's narrow Deployment/Service/ConfigMap surface, and to prove the two-downstream architecture works before any larger swap is considered.

### Architecture findings that shape this plan

- **Single-downstream today.** `IDownstreamMcpClient` is registered as exactly one DI singleton (`ConfigurationExtensions.cs:76`), consumed by `DownstreamToolRegistry`, `GuardedToolRunner`, and `SanitizingToolCaller`. There is no multi-source concept anywhere in this chain — adding a second downstream is a genuine (bounded) code change, not a config toggle.
- **Process command is hardcoded to `"dotnet"`.** `McpGatewayConventions.DownstreamProcess.Command` is a shared constant; `DownstreamMcpClient.CreateTransportOptions()` always spawns `dotnet run --project <path>` or `dotnet <assembly>.dll`. Nothing today can spawn an arbitrary binary.
- **`StdioClientTransportOptions` (from the `ModelContextProtocol` SDK) is already command-agnostic** — `Command`, `Arguments`, `WorkingDirectory`, `EnvironmentVariables` — so spawning a Go binary is fully supported by the transport layer; only InfraGate's own conventions/options are .NET-specific.
- **Downstream stdio auth is InfraGate-private.** The bootstrap-line protocol (`BootstrapStdioClientTransport`, `DownstreamStdioBootstrapGate`, `DownstreamAuthConventions.MetaKey`/`BootstrapLineKey`) is only understood by `InfraGate.McpServer`. A stock Go binary must run with `DownstreamAuth.Required = false`, which the existing code path already supports cleanly (`IsDownstreamAuthRequired()` → `StdioClientTransport` with no bootstrap line). This is not a security regression: the Gateway's own documented priority order (`src/InfraGate.McpGateway/README.md`, "Security Controls") already ranks the downstream token *last*, below trusted launch, containment, human approval, and per-action authorization — none of which this plan touches.
- **`KubernetesPolicyValidator`, `KubernetesPlanBuilder`, and the whole Approvals stack are pure and independent of which MCP process executes anything.** This plan does not touch any of them.
- **Production packaging is a multi-stage Docker build** (`deploy/docker/mcp-gateway.Dockerfile`) publishing `InfraGate.McpServer` and `InfraGate.McpGateway` into a `dotnet/aspnet:10.0-noble-chiseled` runtime image — **no shell, no libc tooling** (this is why the repo hand-rolled a C# health-check binary instead of using curl). Any Go binary added to this image must be a static binary (`CGO_ENABLED=0`) or it will not run.
- **Config generation convention:** the repo generates `.env` files from `deploy/run-profiles.yaml` via typed `InfraGate.RunProfiles/Profiles/*.cs` classes (see `DownstreamAuthProfile.cs`, `KubernetesAdapterProfile.cs`). Repo lesson: *"Don't add new … environment-variable mappings by default — prefer generated run-profile JSON configuration."* New config for the secondary downstream should follow this pattern, not ad-hoc env vars. **Important caveat verified during plan review:** `src/InfraGate.RunProfiles/Rendering/` today contains exactly one renderer, `EnvFileRenderer.cs` — there is no existing TOML output anywhere in the project or in `Directory.Packages.props`. Generating a TOML file (Task 4) is a genuinely new capability for this project, not an extension of an existing renderer — see Task 4 for the resulting scope/dependency implications.
- **`DownstreamMcpClient.CreateTransportOptions()` hardcodes more than just the process name.** Verified: `Command` is read from a `public const string` on `McpGatewayConventions.DownstreamProcess` (not instance data), and `Arguments` is built from an `options.DownstreamAssembly`/`options.DownstreamProject` conditional (`DownstreamMcpClient.cs:217-247`). Spawning the Go binary requires rewriting this method to source Command/Arguments/WorkingDirectory from per-instance data — see Task 6.
- **`McpGatewayOptions.ValidateProductionSafety()` (`McpGatewayOptions.cs:103`, invoked once at `ConfigurationExtensions.cs:45`) throws in Production mode unless `DownstreamAuth.Required == true`** on the single shared options instance. This check must never end up applying to the secondary (always-unauthenticated) downstream — see Task 5's decision to use a separate sibling options type rather than extending `McpGatewayOptions` itself.
- **No Go toolchain exists in this repo today** (no `go.mod`, no Go CI step). This plan introduces the repo's first Go dependency.
- **Repo lesson (explicit, already learned the hard way):** *"Don't add a constructor dependency to a gateway service without updating every test fixture that manually registers it in DI"* — named fixtures: `KeycloakTests`, `SafetyE2EFixture`, `GatewayHttpMcpIntegrationTests`, `GatewayDiWiringTests`. This plan has an explicit task for it so it isn't rediscovered the hard way again.
- **Repo lesson:** *"Don't rely on `IsError=False`... run the downstream binary standalone and inspect its stderr"* when debugging — this shapes Task 1 as a manual standalone spike before any code is written.
- Upstream `kubernetes-mcp-server` is Apache-2.0, pre-1.0 (latest observed tag `v0.0.64`, published 2026-07-10), distributed as a Go module at `github.com/containers/kubernetes-mcp-server/cmd/kubernetes-mcp-server`, installable via `go install ...@<pinned-version>`. Confirmed config surface (from upstream `docs/configuration.md`) includes `read_only`, `enabled_tools`/`disabled_tools`, `denied_resources` — documented example: `enabled_tools = ["pods_list", "pods_get", "pods_log"]`. Exact current tool names/config shape must be **re-verified against the pinned binary**, not assumed from docs, before any code is written (pre-1.0 software can drift between the doc snapshot and the pinned release).

## Request

User decisions (resolve all previously open questions):
1. **Acquisition:** `go install` the official upstream binary, pinned to a specific tagged version, at build time (both local dev and the production Docker image) — not a vendored release-binary download, not npm/PyPI, not a separate Docker image.
2. **Tool scope:** a small, basic, hand-picked set of read-only tools for now — enough to prove the architecture works, not a comprehensive tool surface. Candidate list (see Task 1): `pods_list`, `pods_get`, `pods_log`, `events_list`, `resources_list`, `resources_get`.
3. **Config mechanism:** TOML config file (not CLI-flags-only) for `read_only`/`enabled_tools`, generated via the run-profiles convention.
4. **Install location:** leaning `.tools/bin/` (repo-local, predictable path independent of the caller's `GOPATH`), confirmed during Task 2.
5. **CI coverage:** real integration tests against the actual installed binary, not unit-tests-only.
6. **Version pin:** latest stable tag for now (`v0.0.64` as observed during this research, published 2026-07-10) — no automated-update policy yet.

**Acceptance criteria for this plan's implementation:**
- The Gateway can spawn `kubernetes-mcp-server` as a second stdio MCP subprocess, independently of `InfraGate.McpServer`.
- `tools/list` on the Gateway's MCP endpoint returns the existing InfraGate tools **plus** a small curated set of kubernetes-mcp-server read-only tools.
- Calling one of the new tools returns real cluster data from the same demo namespace/kubeconfig the rest of the stack already uses.
- No kubernetes-mcp-server tool is ever reachable through `request_*`/`execute_approved_plan` — destructive tools are excluded both by the binary's own `read_only` config and by a Gateway-side filter (defense in depth).
- Existing tests, DI wiring, and the mutation/approval path are unaffected.
- Local dev and CI/Docker both acquire the binary reproducibly via pinned `go install`.

## Plan

### Phase 0: Feasibility Spike (no code — do this first, fail fast)

- [x] **Task 1: Verify the pinned binary standalone over stdio**
  **Description:** `go install github.com/containers/kubernetes-mcp-server/cmd/kubernetes-mcp-server@v0.0.64` locally (re-check this is still the latest tag at implementation start; pin whatever is current then). Run it standalone (not through the Gateway) speaking raw MCP JSON-RPC over stdio (same technique the repo already uses for downstream debugging per the existing lesson). Send `initialize` then `tools/list`. Confirm the chosen curated set exists with these exact names, descriptions, and `readOnlyHint`/`destructiveHint` annotations: **`pods_list`, `pods_get`, `pods_log`, `events_list`, `resources_list`, `resources_get`**. If any name doesn't match what the pinned binary actually exposes, adjust the list to the nearest equivalent real tool name — don't force a mismatch. Confirm the TOML schema for `read_only`/`enabled_tools` (config mechanism is already decided — TOML). Confirm the flag/config name for kubeconfig path and whether namespace scoping is supported at this layer, and check what RBAC the existing `.kube/mcp-nginx-demo.compose.config` context actually grants (if it's already scoped to `mcp-nginx-demo` only, cluster-wide tool exposure may be moot).
  **Acceptance criteria:**
  - [x] Exact confirmed tool names + schemas for the six candidate tools are written down, with any substitutions noted (replacing assumptions in this plan).
  - [x] Confirmed TOML schema for `read_only`/`enabled_tools`.
  - [x] Confirmed kubeconfig flag name and behavior against the demo cluster context.
  **Verification:** Manual transcript of the stdio session (or a scratch script) showing `initialize` → `tools/list` → one successful read-only `tools/call`.
  **Dependencies:** None.
  **Files touched:** None (spike only; findings feed into Task 4/5).
  **Estimated scope:** N/A (research).

  **Spike findings (2026-07-12, pinned `v0.0.64`, confirmed still-latest tag at implementation start):**
  - All six candidate tool names exist verbatim on the pinned binary — no substitutions needed: `pods_list`, `pods_get`, `pods_log`, `events_list`, `resources_list`, `resources_get`. All six report `readOnlyHint=true`, `destructiveHint=false`. (Full unrestricted tool list is 13 tools; all 13 are also read-only-hinted, but only the curated six are enabled per Decisions Made.)
  - Confirmed TOML schema (from upstream `pkg/config/config.go` `StaticConfig`, `github.com/BurntSushi/toml` tags): flat top-level keys `kubeconfig` (string), `read_only` (bool), `enabled_tools` ([]string), `disabled_tools` ([]string), `denied_resources` ([]table, not needed for this phase per Decisions Made — only `read_only`/`enabled_tools`/`kubeconfig` are set). **No array-of-tables construct is actually required for this phase's generated config** — this simplifies Task 4's TOML-writer decision (a hand-rolled flat-key writer is sufficient; `denied_resources` array-of-tables support is not needed since it isn't set).
  - `enabled_tools = [...]` verified to restrict `tools/list` to exactly the 6 listed names (tested empty vs. restricted — 13 tools without it, 6 with it).
  - Kubeconfig: both a `--kubeconfig <path>` CLI flag and a `kubeconfig` TOML key exist; TOML key confirmed working end-to-end against the real demo kubeconfig (`.kube/mcp-nginx-demo.config` — regenerated via `scripts/create-demo-kubeconfig.sh` since the previously-committed token had expired; token rotation is expected/routine, not a plan concern).
  - `tools/call` for `pods_list` against the demo kubeconfig returned real live data from the `mcp-nginx-demo` namespace (3 pods, including one in `ImagePullBackOff`) — proves the binary→cluster path works end-to-end.
  - RBAC check: the `infra-gate-mcp` ServiceAccount used by the demo kubeconfig is bound to a namespace-scoped `Role` (`infra-gate-mcp-manager` in `mcp-nginx-demo`), not a `ClusterRole`. `kubectl auth can-i list pods --all-namespaces`, `list nodes`, and `get namespaces` all returned `no`. **This confirms cluster-wide tool exposure is moot** — RBAC already restricts all reads (including anything routed through `resources_list`/`resources_get`) to the `mcp-nginx-demo` namespace regardless of what the Gateway exposes.
  - Additional finding for Task 2: the binary's own `--version` flag prints `0.0.0` (upstream doesn't stamp a build-time version via plain `go install`); use `go version -m <binary>` (reports the `mod github.com/containers/kubernetes-mcp-server v0.0.64 h1:...` line) to verify the pinned version was actually installed, not `--version` output.

### Checkpoint A: Spike confirms the binary runs standalone over stdio and exposes a real, predictable read-only tool set matching (or updating) this plan's assumptions. If the pinned version's tool/config surface differs materially from what's documented, stop and reconcile before writing any Gateway code.

**Checkpoint A: PASSED.** No material differences from the plan's assumptions — proceeding to Phase 1.

### Phase 1: Acquisition & Packaging

- [x] **Task 2: Add a pinned local-dev install script**
  **Description:** New script under `scripts/` (e.g. `scripts/install-kubernetes-mcp-server.sh`), following existing script conventions (explicit `set -u`-safe variable definitions — see the repo lesson about `GATEWAY_APP_UID` breaking under `set -u`). Installs the pinned version to `.tools/bin/kubernetes-mcp-server` (repo-local, predictable path independent of the caller's `GOPATH` — using `GOBIN=$(pwd)/.tools/bin go install ...@<pinned-version>`), confirming during implementation that this resolves cleanly for both local dev and Docker build contexts. Add `.tools/` to `.gitignore`. Document Go toolchain as a new local prerequisite in root `README.md`.
  **Acceptance criteria:**
  - [x] Running the script twice is idempotent and produces the same pinned version.
  - [x] README documents the new Go prerequisite and the install command.
  **Verification:** `./scripts/install-kubernetes-mcp-server.sh && kubernetes-mcp-server --version` (or equivalent) prints the pinned version.
  **Dependencies:** Task 1 (confirms flags/version).
  **Files touched:** `scripts/install-kubernetes-mcp-server.sh`, `README.md`.
  **Estimated scope:** S.

  **Implementation note:** verification uses `go version -m <binary>` (reports the pinned `mod` line) rather than the binary's own `--version` flag, which prints `0.0.0` regardless of pinned tag (see Task 1 finding — upstream doesn't stamp version via plain `go install`). Ran the script twice back-to-back; both runs installed the identical module hash (`h1:hH8XdzeHw+ul9QRyH608Asm+Z+Kpu58QQGOKGNz10H8=`), confirming idempotency. `.tools/` confirmed ignored via `git status --ignored`.

- [x] **Task 3: Add a Go build stage to the production Docker image**
  **Description:** Extend `deploy/docker/mcp-gateway.Dockerfile` with a `FROM golang:<pinned-tag> AS k8s-mcp-build` stage running `CGO_ENABLED=0 go install github.com/containers/kubernetes-mcp-server/cmd/kubernetes-mcp-server@<pinned-version>`, then `COPY --from=k8s-mcp-build /go/bin/kubernetes-mcp-server /app/k8s-mcp-server/kubernetes-mcp-server` into the chiseled runtime stage. `CGO_ENABLED=0` is required because the final `aspnet:10.0-noble-chiseled` image has no libc/shell — the binary must be statically linked.
  **Acceptance criteria:**
  - [x] Image builds successfully with the new stage.
  - [x] `docker run --entrypoint /app/k8s-mcp-server/kubernetes-mcp-server <image> --version` (or `--help`) runs successfully inside the chiseled image (proves static linking works with no shell/libc present).
  **Verification:** Local `docker build` of `deploy/docker/mcp-gateway.Dockerfile` succeeds; the entrypoint-override smoke check above passes.
  **Dependencies:** Task 1.
  **Files touched:** `deploy/docker/mcp-gateway.Dockerfile`.
  **Estimated scope:** S.

  **Implementation note:** used `golang:1.26.5` (matches the Go SDK version validated in Task 1; kubernetes-mcp-server's `go.mod` requires `go 1.26.3` minimum), tag-pinned consistent with this Dockerfile's existing style (`alpine:3.21`, `mcr.microsoft.com/dotnet/sdk:10.0` are also tag-pinned, not digest-pinned). Version is parameterized via `ARG KUBERNETES_MCP_SERVER_VERSION=v0.0.64` in the new stage. `docker build` succeeded; `docker run --entrypoint /app/k8s-mcp-server/kubernetes-mcp-server <image> --version` printed `0.0.0` with exit code 0, confirming the static binary runs inside the shell-less/libc-less chiseled image.

- [x] **Task 4: Add TOML rendering to InfraGate.RunProfiles, then generate the curated-tools config**
  **Description:** Generate a TOML config file following the existing run-profile generation convention: add a `KubernetesMcpServerProfile.cs`-style class under `src/InfraGate.RunProfiles/Profiles/`, wire it into `deploy/run-profiles.yaml`, and have it render the TOML alongside the existing generated `.env` files rather than hand-writing a static config file. The kubeconfig path is passed via whatever mechanism Task 1 confirms (CLI flag or TOML key) pointing at the same demo kubeconfig context already mounted for the primary Kubernetes adapter. Bake in `read_only = true` unconditionally for this phase — not configurable — since nothing downstream is prepared to route mutations through this client. `enabled_tools` is restricted to the six-tool candidate list from Task 1 (adjusted for any name corrections found there), defined in exactly one place (this new profile class) — no second hand-copied list anywhere else in the codebase (see Task 10).

  **New capability, not an extension:** `src/InfraGate.RunProfiles/Rendering/` currently has exactly one renderer, `EnvFileRenderer.cs`, and there is no TOML support anywhere in the project or in `Directory.Packages.props`. This task introduces the project's first TOML output. As part of this task, explicitly decide and record the TOML-writing approach — either add a new centrally-versioned package (e.g. `Tomlyn`) to `Directory.Packages.props`, or hand-roll a minimal writer sufficient for this flat/array-of-tables schema — with the same supply-chain scrutiny this plan already applies to `go install`/GOSUMDB and SHA-pinned GitHub Actions.
  **Acceptance criteria:**
  - [x] `read_only = true` is present in every generated config with no way to disable it via env var.
  - [x] `enabled_tools` is restricted to the curated set confirmed in Task 1, defined in exactly one place.
  - [x] Config generation follows the existing run-profile pattern (no scattered new env vars).
  - [x] TOML writer choice (new dependency vs. hand-rolled) is decided and recorded; if a new package, it is added to `Directory.Packages.props` with the same version-pinning discipline as existing dependencies.
  **Verification:** Generated TOML inspected manually; running the binary with the generated file returns only the curated tool set in `tools/list`.
  **Dependencies:** Task 1, Task 2.
  **Files touched:** `src/InfraGate.RunProfiles/Rendering/TomlFileRenderer.cs` (new), `src/InfraGate.RunProfiles/Profiles/KubernetesMcpServerProfile.cs` (new), `deploy/run-profiles.yaml`, `Directory.Packages.props` (if a new TOML package is added), generated TOML output.
  **Estimated scope:** M.

  **Implementation notes / deviations from the file-touch list above (recorded per custom-implementer workflow):**
  - **TOML writer: hand-rolled**, not a new package. Task 1's spike found the generated config only needs flat scalar keys (`kubeconfig`, `read_only`) and one string array (`enabled_tools`) — no array-of-tables (`denied_resources` isn't set in this phase). A ~15-line hand-rolled writer in `TomlFileRenderer.cs` covers this fully; a dependency like Tomlyn would be pure overhead for a schema this small. `Directory.Packages.props` is therefore **not touched**.
  - **`deploy/run-profiles.yaml` is not touched.** The only per-profile-varying value is `kubeconfig`, which is derived directly from the profile's existing (required, always-present) Kubernetes Domain Adapter — the same value `EnvFileRenderer.AppendKubernetesAdapter` already reads. No new YAML keys were needed since `read_only`/`enabled_tools` are hardcoded constants on `KubernetesMcpServerProfile` (not configurable per the Decisions Made section), so there was nothing new for the YAML schema to express.
  - **CLI wiring:** added a new `generate-toml <profile> --output <path> [--force]` command (`RunProfileConventions.Commands.GenerateToml`) alongside the existing `generate`, reusing the same overwrite-protection logic (`RunProfileCli.HandleGenerateCommandAsync` now takes a `Func<string, RunProfile, string> render` delegate so `generate`/`generate-toml` share write/overwrite-check code instead of duplicating it). Did not repurpose the removed `--format` flag — that was deliberately rejected previously (`RejectRemovedFormatOption`) with the message "generate writes one env file", so reintroducing it for TOML would contradict that decision.
  - **Verification performed:** ran `dotnet run --project src/InfraGate.RunProfiles -- generate-toml local-source-gateway --output deploy/generated/local-source-gateway.kubernetes-mcp-server.toml` (output correctly lands under the already-gitignored `/deploy/generated/` directory, no new `.gitignore` entry needed), then ran the pinned binary against the generated file via the same stdio probe as Task 1 — `tools/list` returned exactly the six curated tool names, confirming the generated config is correct end-to-end. The verification artifact was deleted afterward (regenerable on demand).
  - **New unit tests:** `tests/InfraGate.RunProfiles.Tests/UnitTests/TomlFileRendererTests.cs` (6 tests: empty config name, null profile, missing Kubernetes adapter throws, kubeconfig/read_only/enabled_tools all present, `read_only` non-disableable, `enabled_tools` cross-checked against the single shared `KubernetesMcpServerProfile.EnabledTools` list so Task 10 can source the same list without duplication). Full `InfraGate.RunProfiles.Tests` suite: 148/148 passing.

### Checkpoint B: Binary is acquired reproducibly in local dev and in the Docker image; it runs standalone (outside the Gateway) with the generated config and exposes exactly the curated read-only tool set. Nothing in the Gateway process has changed yet.

**Checkpoint B: PASSED.** Binary installs reproducibly (Task 2), builds into the chiseled Docker image as a static binary (Task 3), and the generated TOML config (Task 4) drives the binary to expose exactly the six curated read-only tools — all verified standalone, outside the Gateway.

### Phase 2: Gateway Wiring

- [x] **Task 5: Add a second downstream-process descriptor as a sibling options type, plus the keyed-DI constant**
  **Description:** Add a small **sibling options record** (e.g. `KubernetesMcpServerProcessOptions`) holding the fields needed to spawn the second process: binary path/command, arguments, working directory, and an explicit `AuthRequired` flag defaulted/hardcoded to `false`. **Do not extend `McpGatewayOptions` itself** — see rationale below. Keep this additive — do not change any existing single-downstream field or `McpGatewayConventions.DownstreamProcess` constants used by the primary client. Add the keyed-DI service key (e.g. `"k8sMcpServer"`) as a single named constant on `McpGatewayConventions` (alongside `DownstreamProcess`) — it must never be a raw string literal repeated across registration and resolution call sites (Task 6, Task 7, Task 8).

  **Why a sibling type, not an extension of `McpGatewayOptions`:** `McpGatewayOptions` is already a single record mixing Auth/Approval/Smtp/primary-downstream concerns; bolting a second, unrelated process descriptor onto it compounds that. More concretely, `ValidateProductionSafety()` (`McpGatewayOptions.cs:103`) throws in Production mode unless `DownstreamAuth.Required == true` on the shared options instance — that check must never apply to the secondary (always-unauthenticated) descriptor. A genuinely separate type sidesteps the question instead of requiring a carve-out inside `ValidateProductionSafety`.
  **Acceptance criteria:**
  - [x] New sibling options type is optional/off-by-default so existing deployments without it are unaffected.
  - [x] `ValidateProductionSafety` on `McpGatewayOptions` is unchanged and gains no code path that could require `AuthRequired=true` on the secondary descriptor.
  - [x] The keyed-DI service key exists as exactly one named constant in `McpGatewayConventions`.
  **Verification:** Unit test constructing the new sibling options type with and without configuration present; unit test confirming `McpGatewayOptions.ValidateProductionSafety` behavior is unaffected by the new type's presence/absence.
  **Dependencies:** Task 4.
  **Files touched:** `src/InfraGate.McpGateway/Configuration/KubernetesMcpServerProcessOptions.cs` (new), `src/InfraGate.McpGateway/McpGatewayConventions.cs`.
  **Estimated scope:** S.

  **Implementation notes:** `KubernetesMcpServerProcessOptions` is a standalone record (`Command`, `Arguments`, `WorkingDirectory`) with `AuthRequired` as a `const bool = false` (no constructor parameter — structurally impossible to set `true`). `FromConfiguration` returns `null` when `Command` is unconfigured (off-by-default), reading from a new `InfraGate:Gateway:KubernetesMcpServer` section (constants added to `McpGatewayConventions.ConfigurationKeys`). Added `McpGatewayConventions.SecondaryDownstream` with `ServiceKey = "k8sMcpServer"` (single named constant, per plan) and `Name = "kubernetes-mcp-server"` for the transport's `StdioClientTransportOptions.Name`. New tests: `tests/InfraGate.McpGateway.Tests/UnitTests/KubernetesMcpServerProcessOptionsTests.cs` (6 tests, including one building a full production `McpGatewayOptions` alongside a populated `KubernetesMcpServerProcessOptions` from the same configuration to prove `ValidateProductionSafety()` is unaffected). Full `InfraGate.McpGateway.Tests` suite: 658/658 passing.

- [x] **Task 6: Generalize `DownstreamMcpClient.CreateTransportOptions()`, then register a second, keyed client/registry/guarded-runner triple**
  **Description:** `CreateTransportOptions()` (`DownstreamMcpClient.cs:217-247`) currently hardcodes `Command = McpGatewayConventions.DownstreamProcess.Command` (a static `"dotnet"` constant) and builds `Arguments` from an `options.DownstreamAssembly`/`options.DownstreamProject` conditional. **Rewrite this method first** so Command/Arguments/WorkingDirectory/auth-required are sourced from an injected per-instance descriptor — the Task 5 sibling type for the secondary client, and the existing `McpGatewayOptions` fields (wrapped in the same descriptor shape) for the primary. This is the genuine, non-trivial part of "generalizing" the client — it changes the constructor/behavior of a shared internal class, not just its DI registration, and should not be treated as incidental to the steps below.

  Once generalized, reuse `DownstreamMcpClient`, `DownstreamToolRegistry`, and `GuardedToolRunner` as-is for both instances — do **not** build a generic N-way multi-downstream abstraction. Register a second instance of each via .NET keyed DI services (`AddKeyedSingleton`), keyed by the constant added in Task 5 (never a raw string literal at the registration call site), explicitly wired with `AuthRequired = false` and a code comment explaining why (InfraGate-private bootstrap protocol; trust relies on trusted launch + process containment instead — matches the Gateway's documented security-priority order).
  **Architecture decision:** Duplicate the existing single-downstream plumbing as a second parallel keyed set (same classes, different constructor data), rather than generalizing `DownstreamToolRegistry`/`GuardedToolRunner` to handle N sources. Rationale: the ask is specifically "one more, alongside" — not a general framework — and AGENTS.md's simplicity principle disfavors speculative configurability beyond what's requested.
  **Acceptance criteria:**
  - [x] Existing (unkeyed) `IDownstreamMcpClient` registration, its consumers, and its observable transport behavior (Command/Arguments for the primary dotnet process) are unchanged after the rewrite.
  - [x] `CreateTransportOptions()` no longer reads `Command` from a static constant for either instance — both come from per-instance descriptor data.
  - [x] New keyed registrations resolve cleanly at startup with the new client pointed at the Go binary, using the Task 5 constant (no raw string literal at the registration or resolution call site).
  **Verification:** `GatewayDiWiringTests` (extended) resolves both the default and keyed services without error; existing `DownstreamMcpClient` unit tests covering the primary (dotnet) transport-options path still pass unchanged after the rewrite.
  **Dependencies:** Task 5.
  **Files touched:** `src/InfraGate.McpGateway/Configuration/ConfigurationExtensions.cs`, `src/InfraGate.McpGateway/McpTransport/Client/DownstreamMcpClient.cs`.
  **Estimated scope:** M–L.

  **Implementation notes / deviations:**
  - Added `internal sealed record class DownstreamProcessDescriptor` directly in `DownstreamMcpClient.cs` (not a new file, per the plan's stated file-touch list) with `ForPrimary(McpGatewayOptions)` and `ForKubernetesMcpServer(KubernetesMcpServerProcessOptions)` factory methods. `DownstreamMcpClient`'s constructor now takes `DownstreamProcessDescriptor descriptor` instead of `McpGatewayOptions options`; `CreateTransportOptions()` and `IsDownstreamAuthRequired()` read entirely from the descriptor (`Command`, `Arguments`, `WorkingDirectory`, `AuthRequired`, `AllowedEnvironmentVariables`) — no more static-constant `Command` read for either instance.
  - Added `McpGatewayConventions.SecondaryDownstream.AllowedEnvironmentVariables` (`PATH`, `HOME`, `TMPDIR`, `TMP`, `TEMP` only — no `InfraGate__*` vars forwarded, since the Go binary gets 100% of its config via `--config <toml>` and understands none of InfraGate's env-var conventions).
  - `ConfigurationExtensions.cs`: primary registration now also does `services.AddSingleton(DownstreamProcessDescriptor.ForPrimary(options));` so the existing `AddSingleton<IDownstreamMcpClient, DownstreamMcpClient>()` auto-injection keeps working unchanged. New `RegisterKubernetesMcpServerDownstream(IConfiguration)` extension method (parallel to the existing `RegisterDownstreamAuth`) is a no-op when `KubernetesMcpServerProcessOptions.FromConfiguration()` returns `null` (off-by-default); when configured, it registers the keyed `IDownstreamMcpClient`/`DownstreamToolRegistry`/`GuardedToolRunner` triple via `AddKeyedSingleton`, all keyed by `McpGatewayConventions.SecondaryDownstream.ServiceKey`. The secondary client is wired with a `new NullDownstreamServiceTokenProvider()` directly (not itself keyed in DI) since it's a zero-dependency no-op and `AuthRequired=false` makes it structurally unreachable for the bootstrap-line path anyway.
  - **Unplanned but necessary fix surfaced by the rewrite:** `tests/InfraGate.Safety.E2E.Tests/SafetyE2EFixture.cs` manually registers `AddSingleton<IDownstreamMcpClient, DownstreamMcpClient>()` (a real DI-auto-constructed instance, not a fake) and would have failed at runtime with the classic "generic MCP SDK error masking a DI resolution failure" repo lesson once `DownstreamProcessDescriptor` became a required constructor dependency. Added the one missing `services.AddSingleton(DownstreamProcessDescriptor.ForPrimary(options));` line there proactively rather than leaving it broken until Task 8 (confirmed via `dotnet build` — this fixture requires Testcontainers/Keycloak so its tests weren't run, but the fix is a straight 1:1 mirror of the same line in `ConfigurationExtensions.cs`). `tests/InfraGate.McpGateway.KeycloakTests` needed no change — it fakes `IDownstreamMcpClient` entirely (`NullDownstreamClient`), never constructing the real `DownstreamMcpClient`.
  - Extended `GatewayDiWiringTests.cs` with `Resolve_KeyedKubernetesMcpServerDownstream_Succeeds`, which builds a container with both the unkeyed primary and the keyed secondary triple registered (mirroring the real `ConfigurationExtensions` registration shape) and resolves all four services successfully.
  - Fixed ~25 test call sites across `DownstreamMcpClientTests.cs`, `DownstreamMcpClientRedactionTests.cs`, `McpGatewayOptionsTests.cs`, and `GatewayHttpMcpIntegrationTests.cs` that directly constructed `new DownstreamMcpClient(options, ...)` — all now wrap with `DownstreamProcessDescriptor.ForPrimary(options)`. No test assertions or behavior changed (verified: same `Arguments`/`Command`/`WorkingDirectory` transport-options assertions still pass).
  - Full `InfraGate.McpGateway.Tests` suite: 659/659 passing (658 pre-existing + 1 new).

- [x] **Task 7: Route `GatewayToolDispatcher` to the secondary read-only source**
  **Description:** Inject the keyed secondary `DownstreamToolRegistry` and `GuardedToolRunner` into `GatewayToolDispatcher`. In `ListToolsAsync`, append the secondary registry's `GetReadOnlyAsync()` tools (via the existing `ToolDefinitionFactory.CreateForwardedTool`). In `IsReadOnlyToolAsync`, check both registries. In `HandleReadOnlyAsync`, dispatch to whichever `GuardedToolRunner` instance owns the tool. Deliberately **never** call `GetDestructiveAsync()` on the secondary registry and never build `request_*` wrappers for it — if the binary's `read_only` config were ever misconfigured, the Gateway must still refuse to expose a mutation path for it. `IsDestructiveToolAsync` stays untouched (primary registry only); any attempted mutation against a secondary tool name falls through to the existing "unknown tool" error, which is the correct default-deny behavior.

  **Implementation note (avoid tripled branching):** rather than hardcoding separate named "primary" and "secondary" branches in each of `ListToolsAsync`/`IsReadOnlyToolAsync`/`HandleReadOnlyAsync`, hold the two `(DownstreamToolRegistry, GuardedToolRunner)` pairs as a small internal fixed-size collection (e.g. a 2-element array) and have those three methods iterate it. This preserves Task 6's "exactly two sources, no N-way config" decision while keeping the read-only dispatch logic in one place instead of duplicated three times.
  **Acceptance criteria:**
  - [x] `tools/list` shows existing InfraGate tools + curated kubernetes-mcp-server tools.
  - [x] Calling a curated tool returns real data routed through the correct client.
  - [x] No `request_<k8s-mcp-server-tool>` wrapper is ever generated, even if the upstream binary's annotations claim a tool is destructive.
  - [x] Primary/secondary read-only dispatch is not implemented as three separate duplicated if/else branches — it iterates a small shared internal collection instead.
  **Verification:** New unit tests in `GatewayToolDispatcherTests` covering merged listing and routing; manual MCP call through the Gateway.
  **Dependencies:** Task 6.
  **Files touched:** `src/InfraGate.McpGateway/McpTransport/GatewayToolDispatcher.cs`.
  **Estimated scope:** M.

  **Implementation notes / deviations:**
  - Added `IServiceProvider serviceProvider` as a new constructor dependency on `GatewayToolDispatcher` — this is the mechanism for **optional** keyed DI resolution (`serviceProvider.GetKeyedService<T>(key)` returns `null` gracefully when the secondary isn't registered, unlike `[FromKeyedServices]`/`GetRequiredKeyedService`, which would throw). This keeps the secondary genuinely off-by-default: the Gateway behaves identically to before when `KubernetesMcpServerProcessOptions` is unconfigured.
  - Added `private sealed record class ReadOnlySource(DownstreamToolRegistry Registry, GuardedToolRunner Runner)` and a `readonly IReadOnlyList<ReadOnlySource> readOnlySources` field (computed once from the primary's required constructor params + the optional keyed secondary) — this is the "small internal fixed-size collection" the plan calls for. `ListToolsAsync`, `IsReadOnlyToolAsync`, and `HandleReadOnlyAsync` all iterate it. `IsDestructiveToolAsync` and the destructive-tool section of `ListToolsAsync` are untouched — they still read the primary `registry` parameter directly, never `readOnlySources`, so the secondary registry's `GetDestructiveAsync()` is never called and no `request_*` wrapper can ever be generated for it, regardless of what the upstream binary's tool annotations claim.
  - `HandleReadOnlyAsync` now does its own scan over `readOnlySources` to find which runner owns the tool (previously trivial — there was only one runner). This duplicates the existence check `IsReadOnlyToolAsync` already did one call earlier in `DispatchDownstreamToolAsync`, but `DownstreamToolRegistry.GetReadOnlyAsync` is memoized after the first real downstream call, so the "duplicate" scan is over an in-memory list, not a second downstream round-trip — not worth a bigger refactor for a 1–2-element list.
  - Test fixture update: the sole hand-wired `GatewayToolDispatcher` construction site (`GatewayToolDispatcherTests.CreateContext`) gained an `IServiceProvider? serviceProvider = null` parameter (defaulting to an empty `ServiceCollection().BuildServiceProvider()` so all existing tests keep exercising the single-source path unchanged). New unit tests: `ListToolsAsync_WithSecondarySource_MergesReadOnlyTools`, `ListToolsAsync_WithSecondarySource_NeverGeneratesRequestWrapperForSecondaryTool`, `CallToolAsync_SecondaryReadOnlyTool_RoutesToSecondaryRunner` (asserts the call reaches the secondary's fake downstream and never the primary's). A new `BuildServiceProviderWithSecondary` test helper registers a keyed `DownstreamToolRegistry`/`GuardedToolRunner` pair to simulate the configured-secondary case.
  - `SafetyE2EFixture.cs` and `KeycloakIntegrationTests.cs` needed **no changes** for this task specifically — both resolve `GatewayToolDispatcher` via `AddSingleton<IGatewayToolDispatcher, GatewayToolDispatcher>()` auto-injection, and `IServiceProvider` is always resolvable from any container without explicit registration. Confirmed via `dotnet build` on both projects.
  - Full `InfraGate.McpGateway.Tests` suite: 662/662 passing (659 pre-existing + 3 new).

- [x] **Task 8: Update every hand-wired test fixture**
  **Description:** Per the existing repo lesson, add the new constructor dependencies to every place that manually builds the Gateway's DI container for tests, or the MCP SDK will mask the real DI error as a generic "An error occurred invoking" string: `tests/InfraGate.McpGateway.KeycloakTests/IntegrationTests/KeycloakIntegrationTests.cs`, `tests/InfraGate.Safety.E2E.Tests/SafetyE2EFixture.cs`, `tests/InfraGate.McpGateway.Tests/IntegrationTests/GatewayHttpMcpIntegrationTests.cs`, `tests/InfraGate.McpGateway.Tests/UnitTests/GatewayDiWiringTests.cs`.
  **Acceptance criteria:**
  - [x] All four fixtures build the Gateway successfully with the new keyed services present (using a fake/no-op secondary client in fixtures that don't need the real binary).
  **Verification:** Full existing test suite still passes after this task.
  **Dependencies:** Task 6, Task 7.
  **Files touched:** the four files above.
  **Estimated scope:** M.

  **Implementation notes / deviations:**
  - **`GatewayHttpMcpIntegrationTests.cs`**: fixed as part of Task 6 (it directly constructs the real `DownstreamMcpClient`, so it needed the `DownstreamProcessDescriptor.ForPrimary(...)` wrap at every call site). Full suite run confirms it still passes (part of the 662/662 `InfraGate.McpGateway.Tests` total).
  - **`GatewayDiWiringTests.cs`**: extended in Task 6/7 with `Resolve_KeyedKubernetesMcpServerDownstream_Succeeds`, which builds a container with both the unkeyed primary and a keyed secondary triple and resolves all four services successfully — this is the "fake/no-op secondary client" case the acceptance criterion describes.
  - **`SafetyE2EFixture.cs`**: needed one line (`services.AddSingleton(DownstreamProcessDescriptor.ForPrimary(options));`, added during Task 6) since it manually registers the real `DownstreamMcpClient` via `AddSingleton<IDownstreamMcpClient, DownstreamMcpClient>()`. It does **not** register a fake secondary — and doesn't need to, because `GatewayToolDispatcher`'s secondary dependency is resolved via optional keyed lookup (`IServiceProvider.GetKeyedService`, added in Task 7), which returns `null` gracefully when nothing is keyed-registered, rather than throwing. Verified for real: ran the full `InfraGate.Safety.E2E.Tests` suite against the already-running local Docker stack (Keycloak, Postgres, minikube) — **18/18 passing**, confirming the fix works end-to-end, not just at compile time.
  - **`KeycloakIntegrationTests.cs`**: needed **no changes**. It fakes `IDownstreamMcpClient` entirely via a local `NullDownstreamClient` (`services.AddSingleton<IDownstreamMcpClient, NullDownstreamClient>()`), never constructing the real `DownstreamMcpClient`, so it was never affected by the constructor signature change. Ran the full Testcontainers-backed suite (spins up its own Keycloak container) to confirm.
  - **Deviation from "using a fake/no-op secondary client in fixtures that don't need the real binary":** none of the four fixtures actually needed a fake secondary registered, because the design choice in Task 7 (optional keyed resolution via `IServiceProvider`, not a required `[FromKeyedServices]` constructor parameter) makes "secondary absent" a fully supported, silently-correct configuration everywhere — not just in fixtures that explicitly opt in. Adding a fake secondary to all four fixtures would have been speculative duplication with no corresponding test-value, since `GatewayToolDispatcherTests.cs` already directly and thoroughly unit-tests the "secondary present" path (3 dedicated tests from Task 7).

### Checkpoint C: Gateway starts locally with both downstreams wired. `tools/list` via the real MCP endpoint shows the existing InfraGate tools plus the curated kubernetes-mcp-server tools. All pre-existing tests still pass.

**Checkpoint C: PASSED (with the live-endpoint proof deferred to Task 10).** All pre-existing tests pass across every affected test project (`InfraGate.McpGateway.Tests` 671/671, `InfraGate.Safety.E2E.Tests` 18/18 against the real running local-oauth stack, `InfraGate.McpGateway.KeycloakTests` 18/19 — 1 pre-existing unrelated skip). The DI graph resolves both downstreams simultaneously (`GatewayDiWiringTests.Resolve_KeyedKubernetesMcpServerDownstream_Succeeds`), and merged `tools/list`/routing behavior is proven at the dispatcher level (`GatewayToolDispatcherTests` — 3 new tests). The one remaining gap — an actual running Gateway process serving the real HTTP MCP endpoint with the real `kubernetes-mcp-server` binary spawned alongside it — is exactly what Task 10's opt-in integration test builds next; deferring the live-wire proof to that dedicated, more rigorous test (which also asserts allowlist exhaustiveness) avoids a redundant one-off manual run here.

### Phase 3: New Tests

- [x] **Task 9: Unit tests for the secondary client's transport assembly**
  **Description:** Test that the generalized transport-options builder produces `Command`/`Arguments` pointing at the Go binary (not `dotnet`), including the `--config`/TOML-path argument, with no InfraGate bootstrap-auth line attached.
  **Acceptance criteria:** [x] Tests cover the TOML-config-path argument assembly and confirm no downstream-auth bootstrap line is sent.
  **Verification:** `dotnet test tests/InfraGate.McpGateway.Tests`.
  **Dependencies:** Task 6.
  **Files touched:** `tests/InfraGate.McpGateway.Tests/UnitTests/*` (new test file).
  **Estimated scope:** S.

  **Implementation notes:** New file `tests/InfraGate.McpGateway.Tests/UnitTests/DownstreamProcessDescriptorTests.cs` (9 tests). Covers: `ForKubernetesMcpServer` points at the configured Go binary path (not `McpGatewayConventions.DownstreamProcess.Command`/`"dotnet"`), `Arguments` includes `--config` + the TOML path, `WorkingDirectory`/`Name` are correctly sourced, and `CreateTransportOptions()` end-to-end never forwards `InfraGate__*`-prefixed env vars (asserted via `Assert.All` over the produced `EnvironmentVariables` keys, plus the existing `GatewayClientSecret`-specific check). The "no downstream-auth bootstrap line is sent" requirement is tested at its true source of truth: `descriptor.AuthRequired == false` — this is the exact private field `DownstreamMcpClient.IsDownstreamAuthRequired()` reads to decide whether to construct a bootstrap line at all (`CreateBootstrapLineAsync`/`IsDownstreamAuthRequired` are both `private`, and actually spawning a subprocess to observe stdin traffic would be a heavier, more fragile integration-style test for no additional coverage — Task 10 covers the real end-to-end spawn). Also added `ForPrimary_ReflectsDownstreamAuthRequired` as a control/regression test proving the primary path still derives `AuthRequired` from `options.DownstreamAuth?.Required` unchanged. Full `InfraGate.McpGateway.Tests` suite: 671/671 passing.

- [x] **Task 10: Opt-in integration test against the real binary**
  **Description:** Mirror the existing `INFRA_GATE_RUN_GATEWAY_INTEGRATION=1` opt-in pattern with a new env-gated test that spins up the real installed `kubernetes-mcp-server`, calls the Gateway's `tools/list` and one curated read-only tool end-to-end against the demo kubeconfig, and asserts (a) the response shape, and (b) that no non-curated or destructive tool is ever listed. Per repo policy this must be a real integration test against the real binary — no mocking of the process. The expected-allowlist for assertion (b) must be read from the same generated TOML/profile output as Task 4 (or from the single Task 4 constant, if exposed), **not** a second hand-copied list of the six tool names — otherwise the two can silently drift the next time Task 1's "adjust to nearest equivalent real tool name" clause fires on a version bump.
  **Acceptance criteria:**
  - [x] Test skips cleanly (not failing) when the binary isn't installed/opted-in.
  - [x] Test asserts the curated allowlist is exhaustive (fails if an unexpected tool appears), sourced from Task 4's single generated list rather than a duplicated literal.
  **Verification:** `INFRA_GATE_RUN_GATEWAY_INTEGRATION=1 dotnet test tests/InfraGate.McpGateway.Tests` passes locally with the binary installed.
  **Dependencies:** Task 2, Task 7.
  **Files touched:** `tests/InfraGate.McpGateway.Tests/IntegrationTests/*` (new test file).
  **Estimated scope:** M.

  **Implementation notes / deviations:**
  - New file `tests/InfraGate.McpGateway.Tests/IntegrationTests/GatewayKubernetesMcpServerIntegrationTests.cs`, declared as `public sealed partial class GatewayHttpMcpIntegrationTests` (the existing class is already `partial`) rather than a standalone class — this reuses the existing JWT/TestServer/MCP-client plumbing (`CreateGatewayServer`, `CreateHttpMcpClientAsync`, `FindRepoRoot`, `CallTextAsync`, `FakeDownstream`, `NamespaceName`, etc.) instead of duplicating ~150 lines of auth/hosting setup for one new test. Test-explorer visibility is under `GatewayHttpMcpIntegrationTests` as a result — noted as a deliberate DRY tradeoff.
  - **One small, backward-compatible edit to the existing file**: added an optional `Action<IServiceCollection>? configureAdditionalServices = null` parameter to `CreateGatewayServer` (invoked once inside `ConfigureServices`), so the new test can register the keyed secondary `DownstreamToolRegistry`/`GuardedToolRunner` without a second, parallel copy of the ~130-line TestServer builder. All existing call sites are unaffected (new parameter is optional and trailing).
  - **Allowlist sourcing (no hand-copied duplicate):** the test invokes the real `InfraGate.RunProfiles` CLI (`dotnet run --project src/InfraGate.RunProfiles -- generate-toml local-source-gateway --output <path> --force`) to generate the actual TOML config, then regex-parses the `enabled_tools = [...]` line back out of the generated file text to build the expected-allowlist array — never a literal C# list of the six tool names. (Considered adding a `ProjectReference` to `InfraGate.RunProfiles` so the test could read `KubernetesMcpServerProfile.EnabledTools` directly — the plan's "or from the single Task 4 constant, if exposed" fallback — but invoking the real CLI instead exercises Task 4's actual generation path end-to-end as a side benefit, and avoids a test-only cross-project reference from a Gateway test project into a CLI tool project.)
  - Test flow: generates the TOML → builds a real `DownstreamMcpClient`/`DownstreamToolRegistry`/`GuardedToolRunner` triple pointed at the installed `.tools/bin/kubernetes-mcp-server` binary → hosts the real Gateway pipeline via `TestServer` with a lightweight `FakeDownstream` primary + the real keyed secondary → calls `tools/list` over the real MCP HTTP transport and asserts (a) both `get_allowed_namespaces` (primary) and all curated tools are present, and (b) every tool the secondary registry actually reports is a member of the parsed curated allowlist, with no `request_<tool>` wrapper ever generated for any of them → calls `pods_list` for real against the demo namespace and asserts a real (non-error) response.
  - **Verified for real, not just described:** ran the test with `INFRA_GATE_RUN_GATEWAY_INTEGRATION=1` (binary already installed from Task 1/2) — passed in 11s against the live minikube cluster. Ran again without the env var — skips cleanly via early return (trivially "Passed", not xUnit `Skip`, matching the repo's existing `INFRA_GATE_RUN_GATEWAY_INTEGRATION` convention). Full suite: 672/672 passing by default (671 prior + this new opt-in test, which no-ops when not opted in).
  - This test is also what fulfills Checkpoint C's deferred live-endpoint proof (see Checkpoint C note above) — it is the first point in the plan where the real Gateway HTTP MCP endpoint, the real `kubernetes-mcp-server` binary, and the real generated TOML config all run together simultaneously.

- [x] **Task 11: Run the real integration test in CI**
  **Description:** Add a pinned `actions/setup-go` step (SHA-pinned commit, per the repo's existing supply-chain policy — no floating `@vN` tags) to `.github/workflows/integration-tests.yml`, install the pinned `kubernetes-mcp-server` version via the Task 2 script (or an equivalent CI-specific install step), and run Task 10's integration test unconditionally (not opt-in) in that workflow, since the whole point is real end-to-end coverage rather than a mocked/skipped check.
  **Acceptance criteria:**
  - [x] `actions/setup-go` is pinned to a commit SHA, not a version tag.
  - [x] The integration test runs on every CI invocation of this workflow and fails the build on a regression (not skipped/best-effort).
  **Verification:** CI run is green on the feature branch, with the integration test visibly executing (not skipped) in the workflow log.
  **Dependencies:** Task 10.
  **Files touched:** `.github/workflows/integration-tests.yml`.
  **Estimated scope:** S.

  **Implementation notes:**
  - Added `actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16  # v6.5.0` (resolved from the `v6.5.0` tag's commit SHA via the GitHub API, matching this repo's existing SHA-pin convention for `actions/checkout`/`actions/setup-dotnet`) with `go-version: '1.26'`, followed by a new `Install kubernetes-mcp-server` step running `./scripts/install-kubernetes-mcp-server.sh` (Task 2's script) — both placed before `Restore`/`Build` so the binary at `.tools/bin/kubernetes-mcp-server` exists before the test steps run.
  - **No new dedicated test step was needed.** The existing "Run gateway integration tests" step already runs `INFRA_GATE_RUN_GATEWAY_INTEGRATION=1 dotnet test tests/InfraGate.McpGateway.Tests/... --filter "Category!=Keycloak"` against the whole project — Task 10's new test lives in that same project, isn't tagged `Category=Keycloak`, and shares the same `INFRA_GATE_RUN_GATEWAY_INTEGRATION` gate, so it now runs unconditionally as part of that existing step once the binary is present. This avoids a redundant, drift-prone second `dotnet test` invocation.
  - Note (unchanged from before this task, called out for completeness): this workflow's automatic `pull_request`/`push` triggers are already disabled repo-wide pending a self-hosted runner coming back online (see the file's existing top-of-file comment) — this is pre-existing repository state, not something this task changes or needs to resolve. "Runs on every CI invocation" refers to every invocation of this workflow (`workflow_dispatch`), matching the acceptance criterion's own wording.
  - Verified the YAML is well-formed (`python3 -c "import yaml; yaml.safe_load(...)"`) and confirmed the resolved SHA is a real `actions/setup-go` commit via the GitHub API before committing to it.

### Checkpoint D: Full test suite (existing + new) is green locally; opt-in integration test passes against the demo cluster; CI decision is made and implemented.

**Checkpoint D: PASSED.** `InfraGate.McpGateway.Tests` 672/672 (including the opt-in Task 10 test, verified both skipping cleanly and passing for real against the live cluster), `InfraGate.RunProfiles.Tests` 148/148, `InfraGate.Safety.E2E.Tests` 18/18, `InfraGate.McpGateway.KeycloakTests` 18/19 (1 pre-existing unrelated skip). CI wiring (Task 11) is implemented and YAML-validated; a live CI run could not be triggered from this environment (no self-hosted runner access, and the workflow's automatic triggers are already disabled pending one — pre-existing repo state, unrelated to this plan).

### Phase 4: Documentation

- [x] **Task 12: Update Gateway docs**
  **Description:** Update `src/InfraGate.McpGateway/README.md` ("Runtime Flow" / "Security Controls") and `.agents/skills/infragate-mcp-gateway/SKILL.md` to describe the new supplementary read-only tool source, its trust boundary (trusted-launch + containment, no downstream token), and the curated tool list.
  **Acceptance criteria:** [x] Docs accurately describe the two-downstream architecture and its guardrail boundary.
  **Verification:** `/skill:verify-readme-docs`-style read-through against the actual implemented code.
  **Dependencies:** Task 7.
  **Files touched:** `src/InfraGate.McpGateway/README.md`, `.agents/skills/infragate-mcp-gateway/SKILL.md`.
  **Estimated scope:** S.

  **Implementation notes:** README.md updated in three places: (1) Runtime Flow's `DownstreamMcpClient.cs` bullet now describes the generalized `DownstreamProcessDescriptor` and the merged `readOnlySources` dispatch; (2) Security Controls gained a new "Secondary downstream (kubernetes-mcp-server) trust boundary" subsection covering the const-false `AuthRequired`, the narrower env-var allowlist, the two-layer read-only enforcement, and the curated tool list sourced from `KubernetesMcpServerProfile.EnabledTools`; (3) Important Contracts gained one bullet stating the secondary never contributes to the Destructive set or `request_*` generation. `SKILL.md` gained an "Optional secondary read-only source (off by default)" note after the existing tool lists — deliberately not merged into the "Read-Only tools (8)" list, since that list documents what's actually exposed by the currently-running demo deployment (which does not have `InfraGate:Gateway:KubernetesMcpServer:Command` configured), and the note explicitly tells the reader to check with the user before assuming the secondary's tools are present. Read through both files against the actual constant names/values in code (`McpGatewayConventions.ConfigurationKeys.KubernetesMcpServerSection`+`CommandKey`, `SecondaryDownstream.AllowedEnvironmentVariables`, `KubernetesMcpServerProcessOptions.AuthRequired`, the six confirmed tool names) to confirm accuracy.

- [x] **Task 13: Write an ADR**
  **Description:** Record the decision: secondary downstream, read-only-only, unauthenticated-at-stdio (trusted-launch only), `go install`-pinned acquisition, curated tool allowlist, explicitly not the full ADR-0021 swap. Follow the existing ADR format/numbering (next number after the highest existing ADR).
  **Acceptance criteria:** [x] ADR documents context, decision, rationale, and consequences, including the explicit boundary that this does not yet resolve the dry-run/diff question blocking the full swap.
  **Verification:** Peer review / user review.
  **Dependencies:** Task 6, Task 7.
  **Files touched:** `docs/adr/00NN-kubernetes-mcp-server-readonly-secondary-downstream.md`.
  **Estimated scope:** S.

  **Implementation notes:** Written as `docs/adr/0033-kubernetes-mcp-server-readonly-secondary-downstream.md` (0032 was the highest existing ADR). Followed the current ADR format used by recent entries (`# NN. Title` / `Date:` / `## Status` / `## Context` / `## Decision` (numbered subsections) / `## Consequences` / `## References`) rather than the older `# ADR-00NN: Title` format seen on ADR-0021 itself, matching the convention actually in use by the most recent ADRs (0025–0032). Verified ADR-0021's actual text before citing it (read the full file rather than trusting the plan's paraphrase) — confirmed it does say "the active roadmap plans to swap `InfraGate.McpServer` with the external `containers/kubernetes-mcp-server` Go binary" and that the swap deletes `InfraGate.McpServer` entirely, so the plan's Context summary was accurate. No central ADR index file exists in this repo to update — each ADR is a standalone numbered file.

### Checkpoint E (Final): All acceptance criteria in the Request section are met. Two downstreams run side by side; the second is strictly read-only, isolated from the approval path, reproducibly acquired, tested, and documented. Ready for user review before any follow-on work toward the full ADR-0021 swap.

**Checkpoint E: PASSED.** All 13 tasks complete; all 6 checkpoints passed. Summary against the Request section's acceptance criteria:
- ✅ Gateway spawns `kubernetes-mcp-server` as a second stdio MCP subprocess, independent of `InfraGate.McpServer`.
- ✅ `tools/list` returns existing InfraGate tools plus the curated kubernetes-mcp-server tools (proven via a real running Gateway + real binary in Task 10's integration test).
- ✅ Calling a new tool (`pods_list`) returns real cluster data from the `mcp-nginx-demo` namespace.
- ✅ No kubernetes-mcp-server tool is reachable through `request_*`/`execute_approved_plan` — enforced in two independent layers (binary's own `read_only=true`, Gateway-side dispatcher never routing the secondary through the destructive path).
- ✅ Existing tests, DI wiring, and the mutation/approval path are unaffected — 672/672 (`InfraGate.McpGateway.Tests`), 148/148 (`InfraGate.RunProfiles.Tests`), 18/18 (`InfraGate.Safety.E2E.Tests`), 18/19 (`InfraGate.McpGateway.KeycloakTests`, 1 pre-existing unrelated skip).
- ✅ Local dev and CI/Docker both acquire the binary reproducibly via pinned `go install` (script idempotency-tested; Docker static-binary smoke-tested inside the chiseled runtime image).

Recorded in ADR-0033. Ready for user review before any follow-on work toward the full ADR-0021 swap.

## Risks and Mitigations

| Risk | Impact | Mitigation |
|---|---|---|
| Pre-1.0 upstream (`v0.0.x`) API/config/tool-name churn between doc snapshot and pinned release | Medium | Task 1 spike verifies against the actual pinned binary before any code is written; re-verify on every version bump |
| `go install` has weaker supply-chain pinning than a checksummed release-binary download (no `DownstreamAssemblyHash`-equivalent verification today) | Medium | Rely on Go module checksum database (GOSUMDB) verification at install time for phase 1; note as a follow-up hardening item if this graduates past proof-of-concept |
| Chiseled runtime image has no shell/libc — a dynamically-linked Go binary would silently fail to start | High | `CGO_ENABLED=0` static build, verified by an explicit entrypoint-override smoke check in Task 3 |
| First Go toolchain dependency in an all-.NET repo (local dev + CI) | Low–Medium | Documented as a new prerequisite (Task 2); CI scope explicitly decided, not silently skipped (Task 11) |
| Test-fixture DI drift masks real startup errors as generic MCP SDK error text | Medium | Explicit Task 8, directly addressing the repo's own documented lesson |
| Namespace/RBAC scope of the new binary's read access unclear until Task 1 | Low | Task 1 spike checks both the binary's own scoping config and the actual RBAC granted by the reused demo kubeconfig context |
| Enabling a second stdio subprocess widens the Gateway's process/attack surface even in read-only mode | Low | `read_only=true` baked in non-configurably (Task 4) + independent Gateway-side filter that never exposes destructive/`request_*` routing for this source (Task 7), as defense in depth |
| `DownstreamMcpClient.CreateTransportOptions()` hardcodes `Command` as a static constant and branches on .NET-specific assembly/project fields — more surgery than plain DI registration | Medium | Explicit rewrite step called out first in Task 6, scoped and estimated (M–L) separately from the DI-registration work |
| `InfraGate.RunProfiles` has no existing TOML renderer or TOML dependency — Task 4 introduces both, which is a new dependency-supply-chain decision, not a config-format extension | Low–Medium | Explicit dependency decision required as part of Task 4's acceptance criteria, reviewed with the same rigor as the plan's `go install`/Actions supply-chain items |
| A second `McpGatewayOptions`-shaped instance could accidentally trip `ValidateProductionSafety()`'s `DownstreamAuth.Required==true` production gate meant only for the primary downstream | Medium | Task 5 uses a separate sibling options type instead of extending `McpGatewayOptions`, so the secondary descriptor is never subject to that validation path |

## Decisions Made (previously open questions)

- **Tool list:** `pods_list`, `pods_get`, `pods_log`, `events_list`, `resources_list`, `resources_get` — Task 1's spike confirms these exact names exist on the pinned binary and substitutes the nearest real equivalent if any don't.
- **Config mechanism:** TOML config file, generated via the run-profiles convention (Task 4).
- **CI coverage:** real integration test against the installed binary, running unconditionally in `integration-tests.yml` (Task 11) — not unit-tests-only, not opt-in-and-skipped.
- **Version pin:** latest stable tag for now — `v0.0.64` as observed during this research (2026-07-10); no Dependabot/Renovate-style auto-update policy yet.
- **Second downstream-process descriptor:** a separate sibling options type (not an extension of `McpGatewayOptions`), added in Task 5 — resolved during plan verification to avoid entangling `ValidateProductionSafety()`'s production-mode `DownstreamAuth.Required` gate with the secondary, always-unauthenticated descriptor.
- **Keyed-DI service key:** a single named constant on `McpGatewayConventions` (Task 5), never a raw string literal at registration/resolution sites.
- **TOML rendering:** decided as part of Task 4, not left implicit — either a new centrally-versioned package (e.g. `Tomlyn`) or a hand-rolled minimal writer, chosen and recorded during that task.

## Remaining Open Questions

All resolved during implementation (2026-07-12):

- **Install path:** confirmed `.tools/bin/kubernetes-mcp-server` resolves cleanly for both local dev (`scripts/install-kubernetes-mcp-server.sh`, idempotency-tested) and the Docker build context (`golang` build stage `COPY`s from `/go/bin/kubernetes-mcp-server` directly, no `.tools/bin` involvement needed in the image at all — the Dockerfile uses its own `GOBIN=/go/bin` inside the build stage). No fallback to `$(go env GOPATH)/bin` was needed.
- **Version pin:** re-confirmed `v0.0.64` was still the latest tag via the GitHub API at actual implementation start (2026-07-12, same day as research) — no drift.
- **TOML writer choice:** hand-rolled (Task 4) — Task 1's spike found the generated config only needs flat scalar keys and one string array (`enabled_tools`); `denied_resources` (which would need array-of-tables support) is not set in this phase, so a ~15-line writer was sufficient and a new dependency like Tomlyn was not warranted.
