[safety] Don't remove intentional redundant safety guards just to reduce complexity - keep them with an explanatory comment and test. (cause: defense in depth)
[mcp-debug] Don't trust `IsError = False` in gateway logs when every tool returns `An error occurred invoking '<toolName>'` — that text is the MCP SDK's generic catch-all for downstream DI/construction failures; run the downstream binary standalone or inspect its stderr to see the real exception. (cause: silent SDK error wrapping)
[containers] Don't ship host-mounted secrets at mode 0600 to chiseled .NET images — the container's `$APP_UID` (e.g. 1654) doesn't match the host UID and reads fail silently inside DI factories; grant ACL `user:<container-uid>:r` with `setfacl` (preserves owner-only perms) or fall back to chmod 0644 for ephemeral dev tokens. (cause: UID mismatch across bind mounts)
[bash-scripts] Don't reference variables under `set -u` that aren't defined locally — `create-demo-kubeconfig.sh` was referencing `GATEWAY_APP_UID` defined only in `setup-development-deploy.sh`, so `prepare_compose_persistence_dirs` aborted before chowning the bind-mount dirs. Define shared constants in every script that uses them. (cause: shared constants implicitly duplicated, not imported)
[keycloak-import] Don't rely on `--import-realm` to merge built-in client scopes — if `clientScopes` is present in the JSON, only those scopes are created; define `profile`, `email`, `roles`, `web-origins`, `acr`, `basic` explicitly when overriding. (cause: import replaces, doesn't merge)
[keycloak-import] Don't put custom scope names in `defaultDefaultClientScopes` — they're validated before the `clientScopes` section is processed and crash startup; keep custom scopes in `defaultOptionalClientScopes` and reference them per-client. (cause: import-time validation order)
[keycloak-import] Don't leave the trusted-hosts policy with both `host-sending-registration-request-must-match` and `client-uris-must-match` set to `false` — Keycloak refuses to start; keep `client-uris-must-match: ["true"]` to restrict DCR redirect URIs. (cause: policy-required validation invariant)
[keycloak-import] Don't request `offline_access` without first defining the `offline_access` realm role and assigning it (directly or via a default composite) to the users — Keycloak rejects the code-to-token exchange with `Offline tokens not allowed for the user or client`. (cause: missing realm-role dependency)
[silent-failures] Don't await audit/side-effect writes inside an if-branch that must return a user-readable message — if the audit write throws (e.g. approval-store directory not writable) the exception replaces the meaningful refusal message and the MCP SDK swallows it as the generic "An error occurred invoking" string. Wrap audit writes in their own try/catch so the refusal is always returned. (cause: approval store permission failure masking dry-run error)
[silent-failures] Don't leave `approvalStore.CreatePlanAsync` and similar store writes uncaught at the top of a tool call — unhandled IO/permission exceptions propagate to the MCP SDK which returns a generic error instead of a useful message. Add try/catch with a formatted return string and log the exception before it disappears. (cause: missing error boundary around file write)
[mcp-observability] When adding ILogger to a class with multiple test-facing constructors, prefer injecting NullLogger<T>.Instance in tests rather than adding "convenience" overloads to production code — keeps production constructors honest and avoids silent no-logging paths in prod. (cause: test usability vs production clarity)
[naming] Don't rename K8s to K8S for Sonar S101 — keep canonical K8s and document false positives instead. (cause: project naming convention)
[docs] Don't make profile glossaries over-strict or boolean-style — model durable grants, artifact digests, and policy wrappers so the design can evolve. (cause: ontology review)
[docs] Don't collapse approval attempts, challenge outcomes, and execution grants — model Approval Challenge, Challenge Outcome, and Approval Grant separately. (cause: approval terminology correction)
[docs] Don't name terminal challenge records Approval Outcome when denials and expiry are included — use Challenge Outcome and reserve Approval Grant for execution authorization. (cause: outcome/grant ambiguity)
[docs] Don't leave glossary terms in discovery order — order lifecycle terminology in the normal reader flow. (cause: glossary readability correction)
[docs] Don't make Challenge Outcome the causal source of Approval Grant — resolving an approved Approval Challenge records the outcome and issues or references the grant. (cause: audit object vs execution authorization)
[docs] Don't keep validating complex lifecycle language with prose alone — add diagrams, relationship tables, and scenario flows once terminology gets dense. (cause: context loss during glossary review)
[skills] Don't let specialist repo skills bypass repository orientation — point them to repo-onboarding when broader context or current docs matter. (cause: skill dependency correction)
[dotnet-build] Don't add MSBuild workaround files before clearing generated bin/obj state after project graph changes — clean generated outputs and retest the exact command first. (cause: stale generated build state)
[docs] Don't treat mutation-approval profile docs as current-state docs — use them as directional target-state and make implementation catch up. (cause: docs direction correction)
[approval-core] Don't delete generic policy-check hooks just because concrete checks are adapter-owned — keep a base contract in Generic Approval Core and concrete policy checks in Domain Adapters. (cause: domain policy ownership correction)
[audit] Don't make execution lifecycle audit events purely generic when the Domain Adapter starts the actual execution — include generic bindings plus adapter-owned execution context. (cause: execution ownership boundary)
[audit] Don't make Domain Adapters persist the Audit Trail directly — let adapters emit adapter audit payloads/events and let the generic audit trail save them. (cause: audit persistence ownership)
[audit] Don't overload execution.started with approval-grant validation data — audit grant and pre-execution gate validation at the gate boundary, then keep execution.started focused on the adapter execution attempt. (cause: lifecycle boundary clarity)
[audit] Don't make generic audit payload contracts know adapter payload schemas — keep the generic slot flexible while Domain Adapters use strong types before serialization. (cause: adapter payload boundary)
[docs] Don't present lack of token passthrough to stdio as a feature — describe downstream auth as a planned client-credentials OAuth boundary instead. (cause: upcoming downstream OAuth flow)
[di-consistency] Don't add a constructor dependency to a gateway service without updating every test fixture that manually registers it in DI — the MCP SDK swallows the resulting InvalidOperationException as "An error occurred invoking '<toolName>'", masking the real error. Check all CreateGatewayServer / CreateTestServer methods across KeycloakTests, SafetyE2EFixture, GatewayHttpMcpIntegrationTests, and GatewayDiWiringTests. (cause: test DI divergence from production)
[branching] Always ask the user which branch to base implementation work on before creating a worktree or starting commits — never assume main; the active feature branch is almost always the correct base. Starting from the wrong base creates a divergent history that requires a conflict-heavy merge to fix. (cause: downstream-mcp-stdio-auth was branched from main instead of feature/configuration-simplification)
[persistence] Don't cross domain ownership boundaries while swapping storage — keep persistence behind the existing Generic Approval Core, Gateway guardrail, and platform Data Protection seams. (cause: persistence scope clarification)
[persistence] Don't preserve file-backed approval storage as a supported runtime adapter when introducing PostgreSQL — replace the Generic Approval Core persistence slice with PostgreSQL only. (cause: vertical-slice persistence scope)
[testing] Don't fake database-backed approval persistence in tests — use PostgreSQL through Testcontainers and classify those tests as integration tests. (cause: real persistence behavior required)
[persistence] Don't put approval canonicalization or integrity semantics in PostgreSQL-specific JSON behavior — keep canonical text/hash generation in the Generic Approval Core so a future database swap remains plausible. (cause: database-agnostic persistence)
[persistence] Don't move approval lifecycle ID generation into the database persistence layer — keep existing Plan/Challenge/Grant ID generation in approval-domain code and persist supplied IDs. (cause: persistence-as-storage clarification)
[configuration] Don't add new approval persistence environment-variable mappings by default — prefer generated run-profile JSON configuration to avoid scattered env vars. (cause: configuration direction)
[architecture] Don't pass approval persistence configuration to InfraGate.McpServer — keep the downstream domain server unaware of Generic Approval Core storage so it remains swappable. (cause: server persistence boundary)
[approval-core] Don't carry file-store challenge mutation semantics into PostgreSQL — persist Approval Challenges immutably and record terminal Challenge Outcomes separately. (cause: immutable approval records)
[approval-core] Don't model execution persistence as only an applied-plan success marker — store blocked, failed, and succeeded execution outcomes explicitly while keeping replay prevention tied to successful execution. (cause: execution outcome coverage)
[approval-core] Don't collapse duplicate grant/applied validations just because PostgreSQL adds execution claims — keep checks in both approval gating and pre-execution gating as defense in depth. (cause: intentional redundant validation)
[approval-core] Don't treat a blocked execution outcome as automatically retryable — model it as terminal for that plan unless a new plan is requested. (cause: blocked outcome semantics)
[approval-core] Don't make `blocked` mean retryable — reserve `execution.blocked` for terminal no-retry plan outcomes and use `failed` or a future status for retryable execution problems. (cause: outcome status semantics)
[persistence] Don't over-grill existing approval/execution semantics when planning a storage swap — only revisit domain behavior where persistence changes expose an actual storage invariant or concurrency gap. (cause: persistence scope drift)
[persistence] Don't make Gateway services depend directly on approval persistence adapters — Gateway should call broader approval-core workflow interfaces while persistence stays behind InfraGate.Approvals. (cause: approval seam correction)
[spiraling] Don't keep fixing cascading errors in silence for hours — after the second or third wave of new errors from the same root-cause change, stop, describe the tradeoff to the user, and ask which direction to take. Never git-revert bulk work without asking. (cause: 2-hour spiral on analyzer fixes that ended with a revert)
[analyzers] Don't change nested public types to internal (CA1034) without verifying every consuming assembly has InternalsVisibleTo — the cascade of CS0122 across 5+ projects takes hours to unwind. Suppress CA1034 and fix incrementally per assembly instead. (cause: convention-class visibility cascade)
[analyzers] Don't rename public methods or change their signatures to satisfy an analyzer rule without searching for all callers first — test projects break. (cause: Render → RenderAsync broke 14 test methods)
